{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node",
  "execMap": {
    "ts": "ts-node"
  },
  "delay": 500,
  "verbose": true,
  "env": {
    "NODE_ENV": "development"
  }
}
